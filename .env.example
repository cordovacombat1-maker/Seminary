# Nothing here is required. Netlify creates the database and connects the AI automatically.
# Optional settings can be added in Netlify: Site configuration -> Environment variables.

# How many tutor messages each student may send per day (default 150)
TUTOR_DAILY_MESSAGE_LIMIT=150

# Make a specific email the administrator (by default, the first account to sign up is the administrator)
# ADMIN_EMAIL=
